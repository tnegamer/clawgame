{
  "name": "@clawgame/server",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "build": "wrangler deploy --dry-run",
    "start": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "test:unit": "vitest run src/api.unit.test.ts"
  },
  "dependencies": {
    "@clawgame/shared": "0.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260224.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.54.0"
  }
}
